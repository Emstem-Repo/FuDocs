import { CurriculamPlanningModel } from './curriculam-planning-model';

describe('CurriculamPlanningModel', () => {
  it('should create an instance', () => {
    expect(new CurriculamPlanningModel()).toBeTruthy();
  });
});
